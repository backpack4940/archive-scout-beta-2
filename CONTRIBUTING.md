# Contributing

Keep changes focused, testable, and cross-platform. Open an issue before large schema or interface changes.

Every pull request should compile the package and run the complete test suite. Network tests must use mocks or fixtures rather than the live Wayback Machine.

Do not remove migration compatibility or alter existing user projects without a documented migration path and backup behavior.
